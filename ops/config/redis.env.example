# Install root:root 0600. Values intentionally blank.
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USER=default
REDIS_PASSWORD=
# Or configure REDIS_SNAPSHOT_PATH in backup.conf when no live export is used.
